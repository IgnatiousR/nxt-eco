import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // images: {
  //   domains: ['ik.imagekit.io'], // image domain
  // },
  // images: {
  //   remotePatterns: [new URL('https://ik.imagekit.io')],
  // },
};

export default nextConfig;
